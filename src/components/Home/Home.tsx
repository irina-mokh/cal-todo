import { Calendar } from '../Calendar/Calendar';

export const Home = () => {
  return (
    <main className="home">
      <div className="container">
        <Calendar />
      </div>
    </main>
  );
};
